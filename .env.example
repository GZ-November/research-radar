# 运行环境：development / test / production
APP_ENV=development
# SQLite 数据库文件位置
DATABASE_URL=sqlite:///data/research_radar.db

# ---- 远程分析模型（DeepSeek 示例）----
# 填了 LOCAL_LLM_MODEL 时优先走本地模型，下面远程配置作为备选
LLM_PROVIDER=deepseek
# 你的 API 密钥（注意保密，不要提交到 git）
LLM_API_KEY=
LLM_MODEL=deepseek-v4-flash
LLM_BASE_URL=https://api.deepseek.com
# 默认推荐 V4 Flash；复杂、质量优先任务可切换 deepseek-v4-pro
# 是否开启深度思考：enabled / disabled
LLM_THINKING=enabled
# DeepSeek 推理强度：high / max；OpenAI 还支持 none / low / medium / xhigh
LLM_REASONING_EFFORT=high
# 单次调用最大输出 token 数
LLM_MAX_TOKENS=4096
# 模型输入上下文窗口（token 数），用于长文稿截断预算；留空按模式取安全默认值
# （远程 60k / 本地 Ollama 28k），模型窗口更小时可手动调低
# LLM_CONTEXT_TOKENS=
# 远程调用超时秒数（与代码默认值一致）
LLM_TIMEOUT_SECONDS=120

# ---- 本地分析模型（Ollama，文稿不出本机；配置后优先于远程）----
# 留空则使用上面的远程模型；示例：qwen3:4b
LOCAL_LLM_MODEL=
LOCAL_LLM_BASE_URL=http://127.0.0.1:11434
LOCAL_LLM_TIMEOUT_SECONDS=300

# ---- 向量模型（embedding，可选；不配置也能用，只是检索退化为纯关键词）----
# 方式一：本地 Ollama（向量不出本机，无需 API Key）
# EMBEDDING_PROVIDER=ollama
# EMBEDDING_MODEL=qwen3-embedding:0.6b
# EMBEDDING_BASE_URL=http://127.0.0.1:11434
# 方式二：远程 OpenAI-compatible API（OpenAI / SiliconFlow / Jina 等）
# EMBEDDING_PROVIDER=openai
# EMBEDDING_API_KEY=sk-...
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_BASE_URL=https://api.openai.com/v1
# 注意：EMBEDDING_PROVIDER 留空即关闭向量检索
EMBEDDING_PROVIDER=
EMBEDDING_API_KEY=
EMBEDDING_MODEL=
EMBEDDING_BASE_URL=
# 向量请求超时秒数与 429/5xx 最大重试次数（指数退避）
EMBEDDING_TIMEOUT_SECONDS=60
EMBEDDING_MAX_RETRIES=3

# 数据目录（论文、快照、缓存）
DATA_DIR=data
# 演示用例数据目录
FIXTURE_CASE_DIR=tests/fixtures/golden_case
# PDF 解析后端：默认 pymupdf（轻量，无需额外安装）；
# 表格/公式密集的文稿可执行 pip install '.[docling]' 后改为 docling（依赖 torch，体积较大）
PDF_PARSER_BACKEND=pymupdf
# 发送给 Crossref API 的联系邮箱（礼貌池），建议填真实邮箱
# 同一邮箱也复用给 OpenAlex 与 Unpaywall（均免费、无需 API Key）
CROSSREF_MAILTO=local@example.invalid
